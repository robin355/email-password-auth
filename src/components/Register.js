import React from 'react';

const Register = () => {
    return (
        <div>


            {/* <form onSubmit={handleRegister}>
        <input onBlur={handleOnBlur} type="email" name="email" placeholder="Your Email" id="" />
        <br />
        <input onBlur={handleOnBlur} type="password" name="password" placeholder="Your Password" id="" />
        <br />
        <button type="submit">Register</button>

      </form> */}
        </div>
    );
};

export default Register;